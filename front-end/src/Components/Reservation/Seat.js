

function Seat({tables}) {
    let list;
    if (tables.length) {
        list = tables.map((table, index) => {
            return (
               <di></di>
            )
        })
    }

    return (
        <label htmlFor="seat" >
            
        </label>
    )
}

export default Seat